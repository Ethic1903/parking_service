# Application mode
APP_ENV=dev

# HTTP server
HTTP_PORT=8080
HTTP_READ_TIMEOUT_SEC=5
HTTP_WRITE_TIMEOUT_SEC=10
HTTP_IDLE_TIMEOUT_SEC=60
HTTP_SHUTDOWN_TIMEOUT_SEC=10

# Optional local YAML config path
CONFIG_FILE=config/local.yaml
